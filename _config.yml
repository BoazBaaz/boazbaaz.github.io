# ==========================================
# JEKYLL SITE CONFIGURATION
# ==========================================

# LANGUAGE & TIMEZONE
lang: en
timezone: Europe/Amsterdam

# ==========================================
# SITE INFORMATION (SEO & Display)
# ==========================================

# Site title - shows in browser tab, sidebar, footer
title: Portfolio - Boas-Bas van der Veen

# Tagline - shows under title in sidebar
tagline: Third-year Game Programming student at Breda University of Applied Sciences

# Description - used by search engines and RSS feeds
description: >-
  Portfolio showcasing game development projects and technical blog posts by Boas-Bas van der Veen.

# Your site URL - MUST match your GitHub Pages URL
url: "https://boazbaaz.github.io"

# GitHub username - enables GitHub links/features
github:
  username: BoazBaaz

# Social links - used in footer, SEO metadata
social:
  name: Boas-Bas van der Veen
  email: boas.bas@gmail.com
  links:
    - https://www.linkedin.com/in/boas-bas-van-der-veen/
    - https://github.com/BoazBaaz
    - https://boazbaaz.itch.io/

# ==========================================
# APPEARANCE
# ==========================================

# Theme mode: 'dark', 'light', or empty (user can toggle)
theme_mode: dark

# Avatar image - shows in sidebar
avatar: assets/img/profile_picture_sqr.jpg

# ==========================================
# FEATURES
# ==========================================

# Table of contents - shows on right sidebar of posts
toc: true

# Comments - disabled (to enable: set provider to 'giscus' and configure)
comments:
  provider:

# PWA (Progressive Web App) - makes site installable
pwa:
  enabled: true
  cache:
    enabled: true

# Pagination - number of posts per page on blog listing
paginate: 10

# Base URL - leave empty unless site is in a subdirectory
baseurl: ""

# ==========================================
# COLLECTIONS
# ==========================================
# Collections are custom content types beyond posts/pages

collections:
  # tabs collection - your navigation pages (projects.md, about.md, etc.)
  tabs:
    output: true
    sort_by: order
  
  # projects collection - your portfolio projects in _projects/
  projects:
    output: true
    sort_by: date
    permalink: /projects/:title/
  
  # NOTE: _posts is built-in to Jekyll, don't declare it here

# ==========================================
# DEFAULT SETTINGS
# ==========================================
# Automatic front matter for content types

defaults:
  # Settings for all blog posts
  - scope:
      path: ""
      type: posts
    values:
      layout: post
      comments: false
      toc: true
      permalink: /posts/:title/
  
  # Settings for all projects
  - scope:
      path: ""
      type: projects
    values:
      layout: page  # Projects use page layout (no project.html exists)
      comments: false
      toc: true
  
  # Settings for all tabs (navigation pages)
  - scope:
      path: ""
      type: tabs
    values:
      layout: page
      permalink: /:title/

# ==========================================
# BUILD SETTINGS
# ==========================================

# SASS compilation - compressed = minified CSS for production
sass:
  style: compressed

# HTML compression - removes whitespace for smaller files
compress_html:
  clippings: all
  comments: all
  endings: all
  profile: false
  blanklines: false
  ignore:
    envs: [development]

# Files to exclude from build
exclude:
  - "*.gem"
  - "*.gemspec"
  - docs
  - tools
  - README.md
  - LICENSE
  - "package*.json"

# ==========================================
# JEKYLL ARCHIVES PLUGIN
# ==========================================
# Auto-generates category and tag pages

jekyll-archives:
  enabled: [categories, tags]
  layouts:
    tag: tag            # Uses theme's tag.html layout
    category: category  # Uses theme's category.html layout
  permalinks:
    tag: /tags/:name/
    category: /categories/:name/

# ==========================================
# MARKDOWN SETTINGS
# ==========================================

kramdown:
  footnote_backlink: "&#8617;&#xfe0e;"
  syntax_highlighter: rouge
  syntax_highlighter_opts:
    css_class: highlight
    span:
      line_numbers: false
    block:
      line_numbers: true
      start_line: 1
